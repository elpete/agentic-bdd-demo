Use TestBox dry-run / discovery before executing the generated tests.
Confirm suite names, spec count, labels if any, and whether the generated structure matches the intended behavior.
Then tell me the targeted command to run next.
